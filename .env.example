DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
PORT=5000
JWT_SECRET=replace-with-a-secure-secret
# Public URL of the deployed backend (used for redirects, email links, etc.)
BACKEND_URL=https://virtualcinemabackend.onrender.com
